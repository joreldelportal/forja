import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.joreldelportal.fitness',
  appName: 'Fitness App',
  webDir: 'dist',
 
};

export default config;
